# Archived FastAPI backend

This folder holds the unused **GrowthPulse/DataPulse** FastAPI prototype (`backend/`) and its setup notes.

Metreka’s production path is **Vite + Supabase Edge Functions + Paddle**. Do not deploy or extend this FastAPI service unless you intentionally revive it.
